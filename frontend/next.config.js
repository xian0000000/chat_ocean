/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true, // optimasi minify
};

module.exports = nextConfig;
module.exports = nextConfig;
module.exports = nextConfig
